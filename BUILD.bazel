# This empty BUILD.bazel file is required so Bazel can treat the root directory as a package.
# This allows Bazel to reference files like requirements_lock.txt and requirements.in from the root.
# Do not remove this file unless you move all such files to a subdirectory with its own BUILD.bazel.
